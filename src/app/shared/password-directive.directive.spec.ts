import { PasswordDirectiveDirective } from './password-directive.directive';

describe('PasswordDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
